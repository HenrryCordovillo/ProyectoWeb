Para iniciar el fronend tiene que Instalar lo siguiente en la carpeta de tienda
npm install -save rxjs-compat
npm install -g @angular/cli
npm install -g typescript
npm install bootstrap jquery @popperjs/core